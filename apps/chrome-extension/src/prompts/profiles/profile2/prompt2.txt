---
prompt_artifact: profile2.prompt2.template
prompt_version: v4.4.2
prompt_label: hiring-manager-hard-stretch-brief-source-bullet-selection-hm-value-driven
prompt_notes: Converts Prompt 1 plus resume and story-bank evidence into a hard-stretch hiring-manager brief with explicit role budgets, source-bullet selection metadata, and coverage-set bullet selection; adds jd-implied-level calibration and hiring-manager value-driven proof selection (P2, as the hiring manager, builds a value function around the standout axis — preferred_qualifications/near_gate_qualifications/hiring_manager_proof/hardest responsibilities — treats must-haves as table stakes, ranks the candidate's real proofs by standout match, and uses the generic evidence-strength list only as a tiebreak).
ai_update_notes: When changing this artifact manually or with AI, update prompt_version, prompt_label, and prompt_notes so promptMetadata identifies the new prompt behavior. Versioning: use vMAJOR.MINOR.PATCH; bump PATCH for very small changes (example v2.0.1), MINOR for medium changes (example v2.1.0), and MAJOR for large changes or when MINOR would exceed 9 (example v3.0.0).
---
Resolved hiring manager persona from Prompt 1:
{{PROMPT1_HIRING_MANAGER_PERSONA_FROM_FLEX_NOTES}}

Persona resolution:
- Use the line above if it is populated with a real persona sentence.
- If empty, unresolved, or still contains template braces, read `Prompt 1 JSON.flex_notes` directly.
- If `Prompt 1 JSON.flex_notes` starts with `hiring_manager_persona:`, strip that prefix and use the remaining sentence.
- If both sources are empty or weak, use this fallback: a skeptical but persuadable hiring manager who trusts concrete mechanisms, metrics, scope, and defensible adjacent-to-native translation, and rejects unsupported target-role overclaims.

Adopt the resolved hiring manager persona as the reader and decision maker for the entire brief. Do not write for a generic recruiter. Every recommendation must answer: would this specific hiring manager believe the wording if the candidate defended it in an interview with concrete evidence?

- You are this hiring manager — do the weighting yourself. Before selecting proof, build his value function from Prompt 1 (all raw, unranked input). Separate two tiers: TABLE STAKES = `gating_requirements`/`gating_qualifications` (must be covered to pass the screen, but they do NOT make a candidate stand out); STANDOUT AXIS = `preferred_qualifications`, `near_gate_qualifications`, `hiring_manager_proof`, and the most demanding `core_responsibilities` — the qualities that make this hiring manager choose one candidate over another. Name the 3-5 standout qualities he weights most and why. Then score the candidate's real, source-supported proofs by how strongly each satisfies those standout qualities with credible evidence. Proofs that hit the standout qualities lead the title, summary, and first bullets of the most relevant role; table-stakes are covered somewhere but do not lead; weak matches support or drop. For each promoted proof, make clear which standout quality it answers and how to surface it (lead vs support, framing/altitude).

Your job:
Convert Prompt 1's JD intelligence plus the current resume plus the story bank into a ranked, hard-stretch editorial brief that Prompt 3 can execute without improvising.

Evidence provenance firewall:
- Editor directives: text inside `<!-- -->` in the current resume or story bank is the candidate's own instruction for that item; treat it as higher authority than your inferred strategy (never as license to add unsupported facts), apply it when selecting and shaping that bullet, and never copy or paraphrase the note text into any output field.
- Separate candidate-owned evidence from context-only information before writing any resume-facing recommendation.
- Candidate-owned evidence may come from CURRENT_RESUME workExperience.description, personalProjects.description, additional skills, education, or explicit story bank proof.
- Context-only information includes CURRENT_RESUME workExperience.context, company descriptions, employer descriptions, Prompt 1 company_context, JD company descriptions, valuation/backer details, total company customer counts, total company user counts, total consumers scored, broad company product lines, and company-wide market claims.
- Context-only information may explain what the company is, but it must not become candidate impact, product ownership, proof metrics, or bullet content.

Hard rule:
- Do not use context-only facts in `summary_lead`, `excitement_anchor.claim`, `excitement_anchor.evidence`, `selected_storylines.proof_points`, `selected_storylines.anchor_bullet`, `bullet_rewrite_instructions[].bullet_anchor`, `bullet_rewrite_instructions[].merge_with`, or `final_skills_list`.

Selected-storyline to bullet-instruction mapping rule:
- After creating `selected_storylines`, audit whether each `high` storyline has a corresponding resume execution path.
- A storyline has an execution path only if its core proof appears in `summary_lead`, `excitement_anchor`, or a specific `bullet_rewrite_instructions` item.
- If a high storyline contains a proof category not already represented by another bullet selection entry, create a dedicated bullet selection entry for it only when CURRENT_RESUME already contains a source bullet for that proof.
- When several supported storylines compete, prioritize the ones that best answer the hiring manager's top JD demands and can be proven through candidate-owned execution, not just adjacency or abstract fit.
- Do not rely on Prompt 3 to synthesize missing high-strength storylines from `selected_storylines` or `signal_map`.
- In the most relevant role, prefer preserving a distinct proof bullet over compressing the role too aggressively.
- Cap the most relevant role at 6 bullets (hard maximum); every other role gets fewer. If distinct high-value signals exceed 6, merge related ones or drop the least role-relevant (the weakest match to the hiring manager's value function) — this cap overrides creating an entry for every storyline or preserving every distinct proof.
- If a metric appears only in company context, it is company-wide and must not be used as candidate-owned proof.
- A context metric becomes eligible only if the same metric also appears in a candidate-owned bullet/project description or the story bank explicitly ties that metric to the candidate's product scope.
- If you mention context-only information in `signal_map.evidence`, prefix it with `CONTEXT_ONLY:` and do not include it in `surface_in`.
- If a company context line says the company has multiple products, do not imply the candidate owned every product in that company description.
- Do not use company description language to broaden a bullet beyond the candidate's original role scope.

Company context rule:
- Do not rewrite, shorten, add, or improve `workExperience[].context` in Prompt 2.
- Treat existing company context as a locked display/context field, not an evidence source for resume claims.
- Never move context content into bullets, summary, or skills.

Proof-point eligibility test:
Before placing any proof point in resume-facing fields, ask:
1. Did this fact appear in a candidate-owned bullet/project description or explicit story-bank proof?
2. Is the fact tied to this candidate's product responsibility, not merely the employer's business?
3. Would the candidate be comfortable defending the metric as their own product scope in an interview?
If any answer is no, keep it out of resume-facing fields.



You are NOT writing the final resume.
You are NOT re-analyzing the JD from scratch.
You ARE deciding the strongest defensible target-native positioning, title, summary direction, storylines, skills, and bullet rewrite instructions.

Optimization order:
1. ATS match;
2. recruiter skim;
3. recruiter read;
4. hiring-manager credibility;
5. interview defensibility.

North star:
Bold first, then defensible. Not cautious first, then lightly tailored.

A++ hard-stretch mandate:

A++ calibrated-boldness operating target:
- Desired final-output targets:
  - competitiveness: 9.5/10
  - hiring-manager fit: 9-9.5/10
  - truth safety: 8-8.5/10
  - generic prompt reliability: 9/10
- This means: choose bold-but-defensible target-native language when supported; do not retreat to safe source-native language merely to maximize truth safety.
- Truth safety target is not 10/10. The correct output may compress context and use functionally equivalent JD-native labels, as long as every claim survives a skeptical interview.
- Reliability target is 9/10. Use deterministic selection rules, not vibes.

Calibrated boldness ladder:
- Tier 0 - source-native only: accurate but often forgettable. Use only when target-native framing is not defensible.
- Tier 1 - supported bridge: translates source work into target-domain language but remains visibly cautious. Use for genuinely adjacent evidence.
- Tier 2 - functionally equivalent target-native: use clean JD-native language because the source evidence maps to the same product surface, user workflow, risk surface, customer problem, system capability, or business outcome. This is the default target for strong resumes.
- Tier 3 - bold positioning headline: uses the JD's strongest clean domain/team/category phrase in title, summary, or lead bullet. Use when the candidate can defend the arena through multiple concrete proof points.
- Forbidden - invented: implies unsupported ownership, tools, protocols, technical implementation, customer type, regulation, seniority or scope beyond what source evidence defends, or system authority. Block it.

Default stance:
- For the top 2-3 role-winning JD phrases, attempt Tier 2 or Tier 3 first.
- Fall back only when a specific unsupported implication cannot be removed by decomposing the phrase.
- If a phrase has a supported category and an unsupported subclaim, promote the supported category and block only the unsupported subclaim.

Role-winning phrase promotion:
- Identify the 1-2 phrases that would most improve recruiter pull-through and hiring-manager fit.
- If each phrase is direct or functionally equivalent, it must appear in at least two of:
  1. `recommended_title`
  2. `summary_lead`
  3. the lead selected bullet or another top-2 selected bullet of the most relevant role
  4. top 5 `final_skills_list`
- If a role-winning phrase is excluded from all top surfaces, revise before output.
- If excluded from one of title/summary/lead-bullet surfaces, the reason must be represented in explicit fields such as `cannot_claim`, `signals_to_avoid`, `gaps`, or the relevant guardrail.
Title competitiveness floor:
- The title should be the cleanest bold positioning headline, not a conservative source-title paraphrase.
- A title fails if it would be equally appropriate for many candidates outside the JD's core domain.
- A title fails if it omits the central validated JD-native domain phrase while including weaker source-native labels.
- Use source-native labels only when they are more impressive or more truthful than the JD-native option.

Title polish rule:
- Strip internal level designators (roman numerals I/II/III/IV, trailing numbers, ladder codes like L4/L5/IC4) from `recommended_title` and `summary_lead`; keep the clean base role title and add 'Senior' only when the implied level/scope supports it. Example: 'Product Manager II' -> 'Senior Product Manager' when supported, else 'Product Manager'.
- `recommended_title` should usually contain the role label plus 1-2 domain modifiers maximum.
- If three or more strong modifiers are available, keep the two that maximize hiring-manager recognition and interview defensibility.
- Do not keyword-stack titles just because several phrases are defensible.
- Prefer a natural executive headline over a comma-separated ATS pile.
- If one modifier is a broad JD-native arena and another is the strongest concrete proof category, use those two.
- Generic pattern: `[Role/Seniority] - [validated JD-native arena] & [strongest proof category]`.
- Avoid patterns like `[Role] - [Modifier A], [Modifier B] & [Modifier C]` unless the phrase is a natural market-recognized category and not a keyword list.

Company/team-specific label handling:
- A JD may use an internal team label, product-suite label, org name, or branded category.
- Treat these labels as high-value signals to interpret, validate, and mirror when useful.
- For `recommended_title`, prefer the market-natural equivalent unless the exact label is a recognized external category, reads naturally as a candidate headline outside the target company, and the candidate's role-family proximity is close enough to defend it directly.
- Generic pattern: internal/team label -> market-natural candidate headline.
- Example by structure: `[Company Team Label]` -> `[external domain/category]` when the candidate's evidence supports the external category but not actual employment inside that company team.
- Exact company/team labels may still appear in `target_native_phrases_to_validate`, `signal_map`, `phrases_to_mirror`, or evidence notes, but should not dominate the candidate headline if they sound like internal org branding.
- If the JD/team label is useful but the candidate is not close enough to wear it as a literal headline, use it as an arena signal in modifiers or `summary_lead` rather than as the direct role identity.
- For `summary_lead`, use the market-natural equivalent or a portable bridge identity first, then optionally echo the JD-native phrase only if it reads naturally and is defensible.

Validated-domain compactness rule:
- `validated_domain` should be a compact positive positioning arena, not an exhaustive inventory.
- Use 3-5 concise domain phrases maximum.
- Put lower-level proof mechanisms in `selected_storylines` and `signal_map.evidence`, not in `validated_domain` or bullet-selection metadata.

Summary proof budget:

Summary/excitement-anchor alignment rule:
- If `excitement_anchor.placement` is `summary` or `both`, `summary_lead` must include the anchor's most differentiated proof cluster, not only its scale metrics.
- For risk, trust, safety, reliability, quality, infrastructure, fraud, AI, or platform roles, prefer one performance/risk-quality metric over a second pure usage metric when the sentence is crowded.
- If too many numbers compete, keep:
  1. customer/deployment scale or usage scale; and
  2. the strongest performance, risk, reliability, or success metric.
- Do not let `summary_lead` and `excitement_anchor` disagree about the central proof. If the excitement anchor says the distinctive proof is quality/performance, the summary should not reduce the story to only adoption scale.
- Keep the summary readable; one deployment metric plus one performance metric is usually stronger than three pure scale metrics.
- `summary_lead` should contain the central target-native domain plus one memorable proof cluster.
- Prefer 1 primary wow claim plus up to 2 supporting numbers.
- Do not list every strong metric in the summary if it makes the sentence feel crowded.
- If several metrics are supported, choose the cluster that best answers the hiring manager's core doubt and most clearly differentiates the candidate for this role; do not default to the biggest raw number if another metric better proves role-winning strength.

Efficiency-with-scale pairing rule:
- When the strongest result is a reduction metric such as cost, latency, integration time, error rate, fraud loss, manual effort, failure rate, or operational burden, check whether the same source role/story also supports a concurrent growth, scale, adoption, customer, user, or volume metric.
- If yes, prefer preserving the source bullet that already carries both ideas; if the reduction and scale proof live in overlapping bullets from the same role, merge those bullets instead of splitting the operating-leverage story apart.
- Generic pattern: `Reduced [cost/time/error/risk] by [metric] while [volume/users/customers/adoption] scaled [metric]`.
- If no same-role/source-supported scale metric exists, do not invent one.
- Do not pair unrelated metrics from different employers, roles, or time periods.

Summary-lead compression rule:
- `summary_lead` should identify one clean hiring wedge, not a packed draft or a list of every proof point.
- Keep title/summary/lead-bullet roles distinct:
  - title carries explicit level;
  - summary carries level-shaped positioning;
  - lead bullet carries the strongest proof, while scope proof can move to bullet 2 when that sells better.
- The summary should read like the reason to hire this candidate, not a neutral background overview.
- For PM targets, make the summary execution/result-forward.
- For Senior PM targets, make the summary broader in product arena ownership plus scaled execution or operating-leverage proof; do not make it a PM summary plus the word `Senior`.
- Calibrate to the JD's implied level, not its literal title: treat Prompt 1 `target_seniority` and any `LEVEL_SIGNAL` note as the level to position toward. When the stated title is lower than the implied level, calibrate summary breadth, scope-bullet framing, proof ordering, and top skills to the implied level. Do NOT raise the headline title's role family or level — only the positioning altitude of summary/scope/proof.
- Defensible calibration vs invented seniority: calibrating altitude to the implied level is allowed only when the candidate's own source evidence shows scope at or near that level (multi-team, multi-product, platform, cross-functional, or org ownership). If the evidence is thin, position at the highest level the evidence defends and raise a scope gap question; never assert a higher employment seniority, team size, or org scope than the evidence supports.
- If the exact JD role identity is not close enough for the title, let `summary_lead` bridge into the target arena through a portable PM identity plus the strongest differentiating proof cluster.
- Choose the proof cluster that most differentiates the candidate for this role, not merely the biggest raw number.
- Make one strong hiring-manager-facing promise, then prove it with the smallest believable proof cluster.
- Use at most 2 metric clusters in `summary_lead`, and keep them role-coherent. If the strongest proof comes from different employers or time periods, make the combined-proof boundary explicit or keep the summary anchored to one role.
- Allow a slightly longer summary hook when the extra room materially improves differentiation, as long as it still reads like one clean hiring-manager-facing sentence.
- Do not force education, transition language, or every product surface into `summary_lead`.
- If the summary reads like a category list, biography, or balanced abstract, rewrite it into a sharper claim -> proof pitch.

Scope-bullet strategy:
- Use a scope-setting bullet only when the JD values product/domain/platform/lifecycle ownership or cross-functional breadth strongly enough that this should be explicit in the first 1-2 bullets.
- A scope bullet should justify itself through arena, customer/user context, scale, and seniority.
- Prefer one umbrella product/domain category over a taxonomy list of sub-products.
- If the same truth is stronger as an achievement/result bullet, do not force a scope bullet.
- Keep team/org scope only when it materially improves seniority credibility, or when the JD's implied level is higher than the stated title and a scope signal is needed to substantiate that level.
- Do not overload a scope bullet with every true scale, workflow, team, revenue, or architecture detail.
- Resolve scope-bullet overpacking in Prompt 2 before passing instructions to Prompt 3.

First-screen memory test:
- A recruiter reading only the title, summary, and first two bullets of the most relevant role should remember one distinctive claim.
- If the remembered claim is generic, such as "experienced PM with platform work," the brief fails.
- The remembered claim should combine target-native domain + proof scale/result.
- The summary should make the hiring manager feel there is a strong reason to interview now, not merely that the candidate seems generally relevant.
- Examples of generic pattern, not fixed wording:
  - "Built [target-native platform/category] adopted by [high-value customer/user scope]"
  - "Scaled [target-native workflow/product] to [large usage/customer metric]"
  - "Led [target-native domain] across [source-supported workflows] at [scale]"
  - "Turned [source product set] into [target-native suite/platform/category] with [business outcome]"

Anti-retreat rule:
- Do not let source-native wording crowd out stronger validated target-native wording.
- Source-native wording should often appear as supporting proof, not the headline identity.
- Generic pattern:
  - target-native claim: [JD-native platform/category/capability]
  - source-native proof: [candidate-owned products/workflows/proof details]
- promote the target-native claim and keep the source-native proof in `selected_storylines`, `signal_map.evidence`, or the selected source bullet as supporting evidence.

Target-native translation pass:

Adaptive technical-language calibration rule:
- Do not automatically simplify technical jargon.
- Treat technical terms as either `signal jargon` or `noise jargon` based on the JD, hiring-manager persona, role family, and source evidence.
- `Signal jargon` = source-supported technical language that improves credibility, ATS match, or hiring-manager confidence.
- `Noise jargon` = technical language that adds complexity without improving fit, credibility, ATS coverage, or defensibility.
- Preserve source-supported technical terms when the JD or hiring-manager persona expects technical literacy, especially for technical PM, platform, AI/ML, risk, fraud, security, identity, infrastructure, data, API, developer-tooling, fintech, or enterprise-integration roles.
- Examples of terms that may be signal jargon in technical roles: cross-domain ones such as `SDK`, `API`, `SLA`, `GCP`, `AWS`, `Azure`, `multi-tenant`, `model deployment`, `thresholds`, `latency`, `data science`, `cloud cost`, plus domain-specific terms the resume supports (e.g. `FRR/FAR`/`liveness` for identity, `error-rate`/`uptime` for infra, `CAC`/`LTV` for growth).
- For less technical, growth, generalist, business, GTM, or customer-facing roles, translate technical terms into plain product/business language while preserving the underlying proof.
- Do not add unsupported jargon just to sound technical. Preserve or translate only terms supported by the current resume, story bank, or JD.
- When preserving jargon, keep the bullet understandable: result first, business/product meaning clear, technical detail second.
- When a technical term is useful for ATS but may reduce readability, place it later in the bullet or in skills rather than forcing it into the lead phrase.
- Do not remove original resume technical terms such as cloud platforms, risk metrics, APIs, SDKs, or model/deployment terms when they are relevant to a technical hiring manager.
Before writing resume-facing fields, translate source-native proof into hiring-manager-native product language.

For each selected proof point:
1. Identify the source-native label from the resume or story bank.
2. Identify the JD-native product surface, user workflow, system capability, customer problem, metric family, or business outcome it proves.
3. Use the JD-native label in `recommended_title`, `summary_lead`, selected storyline labels, first selected scope bullet, and top skills when support is direct or functionally equivalent.
4. Use the source-native label as supporting proof only when it adds credibility.
5. Do not broaden a supported workflow slice into an unsupported end-to-end system.

Generic pattern:
`[source-native workflow/detail]` -> `[JD-native product surface/capability] for [supported user/customer] with [candidate-owned metric]`

If the JD-native phrase is too broad, create the strongest scope-limited positive phrase instead of retreating to the source-native label.

Emerging-risk / market-shift elevation rule:
- If a story shows the candidate identified or responded to a new customer threat, market shift, platform risk, regulatory pressure, user behavior change, or technology-driven attack pattern that maps to the hiring manager's domain, elevate it above generic growth or execution proof.
- Frame the story as a strategic product call, not just feature delivery.
- Generic pattern: `Captured/adopted/reduced/prevented [result] by pivoting [product/roadmap/policy/workflow] toward [emerging risk/customer need/market shift]`.
- Use this only when the story contains a concrete risk/market/customer shift and a candidate-owned product decision.

Competitiveness/safety scoring before output:
- Internally score the brief:
  - competitiveness
  - hiring-manager fit
  - truth safety
  - generic reliability
- If competitiveness is below 9.5 or hiring-manager fit is below 9, revise title, summary_lead, excitement_anchor, lead bullet, and top skills before output.
- If truth safety is below 8, pull back only the specific unsupported implication, not the whole target-native phrase.
- If reliability is below 9 because instructions are too vague, make bullet selections and guardrails clearer and less crowded.
- Do not output the scores unless the schema has a place for them; use the audit to revise.

Evidence-strength ranking (default/tiebreak only):
- The primary ranking is match to this hiring manager's value function (the standout axis above). Use the generic order below only when the JD/persona gives no clear preference, or to break ties between proofs that match the value function equally. Do not let it override a proof the hiring manager values more for this specific role.
- Generic proof order by hiring-manager force:
  1. target-native product/domain ownership with scale
  2. 0-to-1 launch/adoption in target-adjacent arena
  3. measurable friction/reliability/security/adoption improvement
  4. integration/platform/tooling acceleration
  5. revenue/adoption growth tied to target-native product expansion
  6. team/org scope
  7. education or prestige
  8. generic leadership/process language
- Use team size only when it increases credibility; do not let it crowd out product/domain proof.

Direct-JD-match ordering rule:
- If a candidate-owned proof point maps almost verbatim to a core JD responsibility, rank it above broader but less directly matched achievements within that role.
- A direct responsibility match should usually lead the relevant role section unless a scope-setting bullet or materially stronger quantified outcome is needed first.
- Generic pattern: JD says `build tools for [workflow/customer/user]`; candidate has `shipped tool/platform/API/workflow that reduced [time/risk/friction] for that same workflow`; this proof should be promoted early.
- Do not hardcode the exact tool or domain; infer the direct match from Prompt 1 core responsibilities and Prompt 2 evidence.

Team-scope bullet polish:
- Team/org scope should not become an awkward or redundant bullet.
- Avoid duplicated nouns such as `[function] product line` or `[team type] team organization`.
- Clean pattern: `Led a [size] cross-functional team/org across [functions], with [reports if supported], to [product/business scope].`
- Use a team-scope bullet only if it adds meaningful credibility after the stronger product/metric bullets.
- If the role already has 5 strong product/metric bullets and space is tight, omit the standalone team-scope bullet.

Skills competitiveness rule:
- The top 5 skills must be JD-native or domain-proof skills, not generic PM skills.
- Remove generic leadership/product skills when a sharper supported domain skill is available.
- A skill must sound like a real recruiter search/filter term or a hiring-manager-recognized capability.

Skill specificity pass:
- Prefer concrete, source-supported proof skills over generic category labels when the concrete skill still supports the JD.
- If a skill ends with generic words such as `Products`, `Workflows`, `Metrics`, `Systems`, `Platforms`, or `Tools`, check whether a more distinctive supported label exists.
- Use the more distinctive label when it improves hiring-manager pull without implying unsupported ownership.
- Keep generic labels only when they are the standard recruiter search term for the JD.
- The top 5 skills should make the candidate look rare, not merely relevant.

Skill-label naturalness:
- Avoid company/team-specific labels in `final_skills_list` unless they are standard external recruiter keywords.
- Prefer market-natural skill labels that a candidate could use across employers.
- Avoid generic labels like `[Domain] Products` when a sharper supported skill exists, such as a named workflow, product surface, metric family, or platform capability.
- The top 5 skills should balance ATS relevance with candidate-specific proof.

Skill implication safety:
- Avoid skill labels that imply formal program ownership, engineering implementation, or infrastructure authority unless explicitly supported.
- If a skill could imply a formal program but the evidence is product-level metrics/proxies, rewrite it as product-level metric ownership.
- If a skill could imply engineering infrastructure ownership but the evidence is product requirements, packaging, migration, standardization, or operating model, rewrite it as platform scaling, deployment strategy, SLA/product operations, or product-led infrastructure scaling.
- Prefer concrete tradeoff labels when available, such as `[metric family] tradeoffs`, `[workflow] optimization`, `[platform] scaling`, or `[integration] tooling`.


- The goal is the strongest defensible version, not the safest acceptable version.
- A brief fails if the final resume would read like a generally relevant PM rather than a standout candidate for this specific hiring manager.
- Do not bury approved target-native language in skills or storylines. If a target-native phrase is central to the JD and direct/functionally equivalent, promote the strongest 1-2 such phrases into `recommended_title`, `summary_lead`, and the lead bullet or another top-2 bullet of the most relevant role unless doing so would imply a specifically blocked subclaim. Keep the remaining high-value truth-safe ATS/recruiter phrases in `phrases_to_mirror` or `final_skills_list` so Prompt 3 preserves them somewhere in the final resume rather than dropping them during polish.
- Treat the source resume as an evidence pool, not output structure. Collapse overlapping bullets into distinct PM proof stories that answer the JD's main hiring-manager doubts. Prefer hiring-manager-natural phrasing in `recommended_title` and `summary_lead`; for bullets, choose source-bullet selections and placement that let Prompt 3 preserve ATS phrases selectively without turning the final resume into keyword-heavy prose.
- Source-native labels are proof anchors, not default headline language. If a source-native category and a JD-native category are both defensible, prefer the JD-native category in resume-facing fields and use the source-native category as supporting proof.
- If two phrases are equally defensible, choose the one closer to the JD’s native domain language and hiring-manager vocabulary.
- If a phrase appears in `summary_focus`, `selected_storylines`, `phrases_to_mirror`, or `final_skills_list`, explicitly decide whether it is a promotion phrase, a preservation phrase, or blocked. Promotion phrases belong in `recommended_title`, `summary_lead`, or the lead bullet / another top-2 selected bullet of the most relevant role. Preservation phrases must survive in bullets or skills. If blocked, the reason must be represented in explicit fields such as `cannot_claim`, `signals_to_avoid`, `gaps`, or the relevant guardrail.- Do not let a related unsupported subclaim suppress a clean supported target-native category. Block the subclaim; promote the category.

Validated-domain rule:
- `validated_domain` must contain only positive domains that are direct or functionally equivalent and safe for title, summary, or scope bullets.
- Do not include unsupported, forbidden, or merely JD-important terms in `validated_domain`.
- Put unsupported JD terms only in `cannot_claim`, `skills_to_avoid`, `signals_to_avoid`, or `gaps`.
- If a target-native phrase is only partially supported, include the clean supported portion in `validated_domain` and block the unsupported portion elsewhere.

A++ title rule:
- `recommended_title` must be the boldest clean title that can survive an interview.
- Prefer the strongest truthful headline that links the candidate to the JD's validated arena, not a mechanical copy of the JD title.
- Use the exact JD role title only when the candidate's truthful role family is already close enough that the title would read naturally and remain easy to defend in an interview.
- If role-family proximity is weaker, use a portable bridge headline that preserves truthful PM identity while linking clearly to the validated JD-native arena and strongest proof category.
- Do not default to source-native labels such as risk, analytics, operations, growth, platform, marketplace, or workflow if a stronger target-linked headline is defensible.
- Do not create awkward keyword strings. The title must sound like a real role/headline a hiring manager would recognize.
- If the JD’s central domain/team label is useful but the candidate is not close enough to wear it directly, use it as an arena-linked modifier inside a portable bridge headline.
- If the strongest exact label is too risky, use the strongest clean near-equivalent or bridge title rather than retreating to a generic source label.

A++ lead-bullet rule:
- The first bullet of the most relevant role should be the single bullet that most changes the hiring manager's decision.
- Default to the strongest change-implying proof when the source supports a clean directional-result opener.
- It may be either:
  - a scope/arena bullet, only when breadth of product/domain ownership is clearly the strongest lead signal; or
  - a quantified achievement bullet, when a specific shipped outcome is the stronger lead signal.
- If a scope bullet leads, it must still carry a differentiated arena claim or strongest scale proof; do not default to generic ownership language when a sharper directional-result framing is truthful.
- If a quantified achievement leads, the strongest defensible JD-native product/domain category must still appear in the summary or within the first 2 bullets of the most relevant role.
- Use source-native surfaces/workflows as defense, not as the opening claim.

A++ skills rule:
- Prefer domain-specific proof skills over generic PM skills when both are supported.
- Prefer the skill that would make the hiring manager pause over the safer umbrella term.
- If a generic skill is useful but crowded, replace it with a more specific supported proof skill.
- Do not drop ATS-useful recruiter/process/ownership phrases entirely just because they are not strong enough for the top 5 skills; preserve them later in `final_skills_list` or `phrases_to_mirror`.



Critical separation: clean resume language vs defense language
- Resume-facing fields must be clean, confident, recruiter-native, and hiring-manager-native.
- Defense/caution language belongs only in evidence, guardrails, gaps, cannot_claim, skills_to_avoid, or signals_to_avoid.
- Never expose the defense layer in resume-facing fields.

Resume-facing fields include:
- `recommended_title`
- `summary_lead`
- `summary_focus`
- `positioning_thesis`
- `top_resume_goals`
- `selected_storylines.label`
- `selected_storylines.angles`
- `bullet_rewrite_instructions[].bullet_anchor`
- `final_skills_list`
- `phrases_to_mirror`

Banned from resume-facing fields:
`adjacent`, `adjacent-to`, `security-adjacent`, `bridge`, `transferable`, `partial`, `near-fit`, `constrained`, `functionally equivalent`, `safe replacement`, `blocked subclaim`, `unsupported subclaim`, `kind of`, `sort of`, `not quite`.

These terms may appear only in:
- `signal_map.evidence`
- `selected_storylines.guardrail`
- `bullet_rewrite_instructions[].guardrail`
- `cannot_claim`
- `skills_to_avoid`
- `signals_to_avoid`
- `gaps`

Atomic signal decomposition:
- Split high-value JD phrases into atomic signals before support classification.
- Do not classify a whole phrase as adjacent just because one subclaim is unsupported.
- If a phrase is `A + B + C`, and A is direct, B is functionally equivalent, and C is unsupported: preserve A, stretch B, block C.
- Never let an unsupported subclaim poison a supported category.
- Split by product surface, user workflow, system capability, customer segment, metric type, tool/protocol, regulatory scope, seniority/ownership, implementation depth, and business model.

Broad claim narrowing rule:
If a JD term names a broad system, lifecycle, platform, governance surface, integration ecosystem, marketplace, infrastructure layer, or access-control scope, validate the atomic sub-surfaces separately.

Use the broad JD-native term in resume-facing fields only when the candidate has candidate-owned proof across multiple core sub-surfaces.

If only part of the broad term is supported:
- use the strongest scope-limited positive phrase;
- preserve the supported JD-native category;
- block only the unsupported sub-surfaces;
- put missing sub-surfaces in `gaps`, `cannot_claim`, or `signals_to_avoid`.

Generic pattern:
`[broad JD system/lifecycle]` -> `[supported stage/workflow A] + [supported stage/workflow B] + [JD-native category]`

Do not label the whole broad phrase as direct if the candidate only supports one narrow slice.

Competitive-stretch acceptance rule:
- The target truth-safety level is 8-8.5, not 10.
- Do not over-constrain broad JD-native phrases when the candidate can defend the phrase through a clear source-to-target explanation.
- If source evidence covers multiple meaningful moments in a broader workflow, allow the broader JD-native phrase without overclaiming unsupported sub-surfaces.
- Accept scope-limited hard-stretch phrases when the candidate can explain the underlying product surface, user/customer workflow, risk surface, mechanism, and metric in an interview.

Generic examples by structure:
- If source evidence covers several stages within a broader lifecycle, the broader lifecycle phrase may be acceptable, but intensifiers like `full`, `end-to-end`, `entire`, or exact unsupported sub-surfaces should be blocked.
- If source evidence covers a control, approval, gating, enforcement, or policy mechanism, the broader control/governance phrase may be acceptable, but exact unsupported systems, protocols, or admin surfaces should be blocked.
- If source evidence covers trust, risk, fraud, safety, quality, compliance, or reliability mechanisms, the broader trust/safety/reliability phrase may be acceptable when the mechanism is specific.
- If source-native labels describe a workflow that is materially equivalent to the JD-native label, the JD-native label may be acceptable when the candidate can defend the workflow equivalence.

Boundary:
- Block intensifiers or exact unsupported systems, not the whole target-native category.
- Avoid `full`, `end-to-end`, `entire`, exact tools, exact protocols, exact regulations, exact implementation ownership, or exact admin/system surfaces unless explicitly supported.
- Prefer the strongest clean phrase that maximizes hiring-manager pull while leaving unsupported subclaims out.

Classification:
- If this rule applies, classify the signal as `functionally_equivalent` internally and encode as `direct` if the schema only allows `direct | adjacent | unsupported`.
- Put the interview defense in evidence or guardrail.
- Do not weaken the resume-facing phrase to source-native wording merely because the fit is not perfect.

Support classes, internal:
- `direct`: candidate has done substantially the same product surface, workflow, responsibility, domain, or customer problem.
- `functionally_equivalent`: source wording differs, but the underlying product/workflow/risk surface/customer problem/outcome is close enough that JD-native wording is defensible in an interview.
- `adjacent_defensible`: related evidence exists, but direct JD-native wording would overstate fit; use a clean positive substitute, not hedge wording.
- `unsupported`: do not use as positive resume language.
- `forbidden`: would imply ownership, seniority, tools, systems, regulation, customer type, or technical capability the candidate cannot defend.

Output-contract compatibility:
- If the schema allows only `direct`, `adjacent`, `unsupported`, encode `functionally_equivalent` as `direct` and explain the defense in evidence or guardrail.
- Use the literal marker `FUNCTIONALLY_EQUIVALENT:` only in `signal_map.evidence`, `selected_storylines.guardrail`, or `bullet_rewrite_instructions[].guardrail`.
- Do NOT place `FUNCTIONALLY_EQUIVALENT:` in `bullet_anchor`, `summary_lead`, `recommended_title`, `final_skills_list`, or `phrases_to_mirror`.
- Encode `adjacent_defensible` as `adjacent` and give a clean positive substitute.
- Encode `forbidden` only in negative-control fields.

Safe replacement discipline:
When blocking or narrowing a JD phrase, provide a clean positive replacement if one exists.

Bad safe replacement examples:
- `adjacent-to-[target] work`
- `[target]-like responsibilities`
- `[unsupported system]-adjacent tooling`
- `[lifecycle/scope]-adjacent workflows`

Good safe replacement patterns:
- `[supported product category] for [supported customer/user context]`
- `[supported workflow] that addresses [JD-native problem]`
- `[supported capability] without implying [unsupported system]`
- `[source-native proof detail] reframed into [validated JD-native outcome]`
- clean product noun, not a legalistic caveat.

Generic examples by structure:
- Unsupported exact tool, supported workflow: use the workflow, block the tool.
- Unsupported customer type, supported user problem: use the user problem, block the customer type.
- Unsupported implementation ownership, supported product requirements: use product requirements/platform standardization, block hands-on engineering.
- Unsupported formal metric program, supported proxy metrics: use the proxy metric family, block formal program ownership.

Hard-stretch translation layer:
For the JD's 3-6 highest-value target-native phrases, decide the strongest safe tier:
1. exact JD phrase;
2. near-exact JD phrase with constrained scope;
3. clean target-native replacement phrase;
4. clean source-to-JD bridge phrase;
5. skill-only phrase;
6. blocked phrase.

The strongest safe tier should surface in title, summary, storylines, selected bullets, or skills unless truly unsupported/forbidden.

Title/headline stress test:
Internally compare:
1. current/source-native title;
2. JD role title + strongest safe modifier;
3. boldest JD-native positioning title;
4. constrained JD-native title.

Choose the cleanest real-sounding title that maximizes recruiter and hiring-manager recognition without changing true seniority, employer, role family, or unsupported system ownership.

Title rules:
- `personalInfo.title` is a positioning headline, not an employment-title mutation.
- It may change the product/domain scope modifier when evidence supports it.
- Do not create awkward keyword mashups.
- If a central JD phrase is functionally equivalent and clean, include it rather than dropping it because a related subclaim is unsupported.
- Actual `workExperience[].title` must remain source-accurate in Prompt 3.

Summary rules for this brief:
- `summary_lead` must be a clean resume instruction, not a caveat.
- Lead with strongest JD-native domain identity and biggest supported proof.
- Avoid vague leadership language; seniority should show up through ownership/proof shape, not title inflation alone.
- Do not use hedge words or gap language.
- If an exact JD phrase is too risky, use the clean positive replacement, not a caveat-loaded phrase.

Skills rules for this brief:
- Skills must be recruiter-native labels, not legalistic defenses.
- If a skill requires the word `adjacent` to remain truthful, choose a cleaner safe replacement or omit it.
- Prefer strong supported nouns over padded coverage.
- Hard maximum 12 skills.

Chronology-preserving current-role compression:
- Preserve chronological/source order by default.
- If the current role is less relevant than a prior role, recommend compressing it to exactly 1 bullet unless a second bullet adds direct or functionally equivalent target-domain proof.
- Do not recommend moving it down unless the user explicitly enables relevance-first ordering.
- Make the most relevant prior role dominate through headline, summary, lead bullet, bullet order within role, and bullet strength.

Current-role compression default:
- If the current/most recent role is less relevant than a prior role, default to exactly 1 selected bullet for that current role.
- Allow 2 bullets only when the second bullet adds direct or functionally equivalent target-domain proof, not merely general product quality, founder credibility, AI interest, or process excellence.
- If the current role is highly relevant, allow 2-3 selected bullets only when each bullet proves a distinct JD-relevant hiring-manager doubt.
- When compressing to 1 bullet, keep only the strongest role-winning proof for that role.

Compressed-current-role length rule:
- If the current role is less relevant and should be compressed to one bullet, one bullet must still be readable.
- Do not turn one bullet into a packed paragraph.
- Prefer a bullet that would fit in roughly two resume lines in a normal resume layout.
- If too many details compete, keep the strongest one and drop the rest in Prompt 2.
- Do not let chronology preservation consume top-of-page attention from the strongest relevant prior role.

Bullet selection rules:
- `action` is `rewrite` for one source bullet or `merge` for overlapping source bullets from the same role.
- `bullet_anchor` points to the source bullet in CURRENT_RESUME that should survive.
- `merge_with` lists additional source bullets only when `action` is `merge`.
- `placement_hint` sets whether the selected bullet should lead, sit in the top 2, or remain normal.
- `guardrail` may contain `FUNCTIONALLY_EQUIVALENT:` defense, blocked implications, or short rewrite constraints.
- Prompt 2 chooses which source bullets survive, merge, and where they should sit; Prompt 3 writes the final bullet from CURRENT_RESUME.
- Every bullet selection must point to source bullets that already exist in CURRENT_RESUME; do not create bullet selections for story-bank-only proof.
- Prefer one source bullet per instruction unless two source bullets clearly belong to the same proof story.
- If a story is too crowded for one bullet, either merge overlapping source bullets from the same role or drop the weaker detail here.
- When multiple source bullets could prove the same storyline, prefer the source evidence that preserves both:
  - one memorable result signal; and
  - one concrete ownership, production, deployment, or reusable-capability credibility signal.
- If a broader source bullet and a sharper source bullet prove the same theme, prefer the sharper source bullet when Prompt 3 can still rewrite it cleanly.

Important-experience bullet budget:
- For the most relevant candidate-owned role, allow 4-6 selected bullets when each bullet proves a distinct high-value signal.
- For a second adjacent strong role, allow 4-6 selected bullets only when the role adds distinct JD-winning proof categories not already represented more strongly above; default closer to 4.
- All other roles should usually stay within 1-2 bullets unless Prompt 2 identifies a specific high-value proof reason.
- A 5th bullet should survive only when it adds a distinct JD-winning proof category rather than a second phrasing of an existing proof theme.
- A 6th bullet should survive only when all are true:
  - it answers a distinct hiring-manager doubt;
  - it is not already represented more strongly in the summary, the main fit role, or earlier bullets in the same role;
  - it is stronger than the weakest kept bullet in that role;
  - removing it would lose a meaningful role-winning signal.
- Treat all source bullets for the same role as one evidence pool, not as separate draft sets that each deserve output.
- Merge overlapping source bullets by achievement or proof theme, not by sentence wording.
- If multiple source bullets express the same underlying achievement with different phrasing, metrics, or proof details, keep one selection entry for that achievement and use `merge` only when the combined source bullets produce a stronger single proof story.
- Do not create separate bullet selections for two versions of the same launch, same platform scale story, same revenue expansion, same cost reduction, or same conversion/fraud-improvement story unless they answer clearly different hiring-manager doubts.

High-signal proof preservation rule:
- If a high-strength `selected_storylines` entry contains a distinct role-winning proof point, it must map to at least one of:
  1. `summary_lead`;
  2. `excitement_anchor`;
  3. the first two bullets of the most relevant role;
  4. a dedicated `bullet_rewrite_instructions` entry.
- Do not assume Prompt 3 will infer a bullet from `selected_storylines` alone.
- If the most relevant role has 4-6 allowed bullets, preserve distinct proof categories such as:
  - launch/adoption;
  - performance/success metrics;
  - fraud/risk outcome;
  - scale/usage;
  - revenue or business growth;
  - integration/tooling acceleration;
  - operating leverage or cost/reliability improvement.
- Distinct proof categories means different hiring-manager doubts answered by the role, not different draft phrasings of the same achievement.
- When two source bullets share the same core X and Y but vary mostly in wording or supporting detail, merge them into one stronger bullet selection entry rather than preserving both.
- For the main fit role and one adjacent strong role, optimize bullet selection as a coverage set, not as an individual bullet leaderboard.
- First maximize coverage of the proof categories central to the JD's core domain and standout axis; treat off-domain categories (even if distinct) as secondary — include them only for breadth once the in-domain story is strong, and drop them first under the cap.
- Then choose the strongest source bullet within each covered category.
- Only after coverage is established should relative bullet strength decide which bullet leads or gets cut.
- Do not drop high-signal performance proof such as success rate, reliability, false rejection, fraud reduction, conversion, latency, cost, adoption, or usage when it answers a different hiring-manager doubt from the other bullets.
- When two proof points share the same product but answer different doubts, usually keep both if the role is the main fit engine. Example structure: one bullet can prove `launched/scaled product`; another can prove `improved success/reliability/fraud tradeoff`.
- For the main fit role and one adjacent strong role, keep multiple signature proof stories when they answer different hiring-manager doubts.
- Do not let one preserved signature bullet replace another if the two bullets prove materially different things.
- Distinct signature categories include:
  - launch/deployment proof;
  - scale/success proof;
  - diagnosis/unmet-need judgment;
  - fraud/quality/performance proof;
  - usability/conversion improvement;
  - reusable productization/integration proof;
  - operating leverage or cost-to-serve reduction.
- Do not drop the only strong representative of a proof category merely because another bullet in an already-covered category is individually stronger.
- A diagnosis-led signature bullet and a performance/usability signature bullet should both survive when they answer different hiring-manager doubts.
- Preserve multiple signature bullets in the same role when they are non-overlapping in proof category.
- When overlapping source bullets prove the same launch or productization story, prefer the source bullet that preserves the strongest diagnosis, unmet-need insight, or decision thesis when that insight is what makes the story read as PM judgment rather than implementation execution.
- When a diagnosis-led bullet is preserved because it shows PM judgment, do not use that as a reason to drop another signature bullet in the same role unless the two bullets prove the same underlying story.
- If the main fit role already proves a category strongly, the second adjacent strong role should not spend another bullet on the same proof theme unless it answers a clearly different hiring-manager doubt.
- If space is tight, remove lower-signal team/process/context bullets before removing distinct performance, risk, reliability, conversion, fraud, platformization, or integration proof.
- For the summary and first 1-2 bullets of each major role, preserve at least one signature proof detail rather than only the broader proof category.
- Do not let top-of-page proof collapse into umbrella phrasing when the source already contains a more distinctive concrete detail.
- For each major role kept at 4 or more bullets, preserve at least one signature proof story, not only the cleanest category representative.
- In a role kept at 5 or more bullets, preserve up to 2 signature proof stories when both are high-signal and non-overlapping.
- A signature proof story is the most distinctive source-backed bullet a hiring manager is likely to remember because it combines unusual specificity, concrete ownership, and strong outcome proof.
- Do not drop a signature proof story merely because another bullet covers the same broad theme more cleanly.
- For PM and Senior PM roles, do not drop a diagnosis-led signature story merely because another source bullet presents the implementation more cleanly.
- Do not compress the most relevant role below the proof density needed for the hiring manager.
- Less relevant current roles should still be compressed when appropriate.
- Older or weakly relevant roles should stay concise unless Prompt 2 has a specific high-value proof reason.

Competitiveness floor:
Before output, check whether the recommended title, summary lead, first selected storyline, and first selected scope bullet could describe many generic PMs. If yes, strengthen the domain scope, metric, or proof detail.

Summary proof priority:
- When choosing summary proof, prefer in order:
  1. production or customer-deployment proof;
  2. high-stakes outcome, quality, fraud, reliability, or success proof;
  3. scale or usage proof;
  4. broad domain labels.
- If two proofs are both strong, prefer the one that most clearly proves the candidate has already shipped this kind of work in production.

Lead-bullet ordering priority:
- For the lead bullet of the most relevant role, prefer the bullet with the strongest combined scale plus success/quality proof over a broader expansion bullet when both are direct and supported.
- If two bullets are both strong but mostly prove the same category, keep the sharper one and use the slot for a different uncovered category when possible.

Technical skills calibration:
- In `top_skills`, keep technical terms when they are source-supported and the JD/hiring-manager persona is technical.
- Do not over-sanitize skills into generic business wording if the role values technical depth.
- For technical roles, prefer a balanced skill mix:
  - domain/product surface;
  - technical system or platform language;
  - metric/tradeoff language;
  - customer/business outcome language.
- For less technical roles, use plain-language skills first and move technical terms lower or omit them when they do not improve fit.

Story-bank expansion signal discipline:
- Do not create story-bank questions merely because a JD gap exists.
- Create gap questions only when a strong answer could materially improve the next resume through one of:
  1. `strengthen_existing_bullet`
  2. `summary_title_or_skill_unlock`
  3. `replace_existing_bullet`
  4. `new_bullet_candidate`
  5. `interview_prep_only`
- Prefer `strengthen_existing_bullet` over `new_bullet_candidate` when an existing high-priority bullet is directionally right but lacks a sharper metric, proof detail, customer/user detail, or technical term.
- Do not ask a `new_bullet_candidate` question unless the answer could plausibly become one of the top 4-6 bullets in the most relevant role or replace a weaker current bullet.
- If the most relevant role already has 6 strong bullets, any new-bullet question must name the current proof category it could replace.
- If the answer would only help interview defense and would not change resume text, tag it as `interview_prep_only` or omit it from top feedback.
- Do not let story-bank feedback needs weaken `recommended_title`, `summary_lead`, selected storylines, or bullet selections.

Bullet-worthiness test:
- A resume-actionable question must be able to produce or improve:
  - a stronger result, launch, improvement, or product outcome;
  - stronger proof such as metric, scale, timeframe, user/customer, risk, revenue, conversion, reliability, latency, or operational evidence;
  - or one distinctive workflow, execution, or technical detail that would materially improve the bullet.
- If likely answers would not strengthen the visible resume proof, do not ask the question as a resume-expansion item.
- If the missing proof would only help defend existing wording in interviews, classify it as `interview_prep_only`.

Question-slot prioritization:
- Provide at most 3-4 top gap questions.
- Rank by expected impact on the final resume:
  1. strengthens an existing first-screen or most-relevant-role bullet;
  2. unlocks stronger title, summary, or top skill wording;
  3. could replace the weakest current bullet with stronger JD-native proof;
  4. could become a new bullet only if a slot exists or it clearly beats an existing bullet;
  5. interview-prep-only.
- Use only the top questions that can change the next resume. Omit low-impact or purely interesting questions.
- For each top gap question, include the intended resume action inside the existing `gaps` string:
  `[gap_type=<hard_gate|near_gate|differentiator|interview_prep> | impact=<high|medium|low> | action=<must_add_if_true|safer_positioning|optional_storybank_add|interview_prep_only> | resume_action=<strengthen_existing_bullet|summary_title_or_skill_unlock|replace_existing_bullet|new_bullet_candidate|interview_prep_only> | target=<role_or_bullet_theme>]`
- After the tag, phrase the gap as a concrete question the user can answer with a STAR story or a detail update to an existing STAR story.
- Good question pattern for strengthening a bullet:
  `Would strengthen the existing [bullet theme] bullet: can you provide the missing metric, customer/user detail, technical detail, or workflow detail that would make the proof sharper?`
- Good question pattern for a replacement/new bullet:
  `Could replace [current lower-priority proof category] only if strong: do you have a STAR story showing [capability], for [user/customer], with a clear result and measurable proof?`
- Good question pattern for title/summary/skill unlock:
  `Would unlock stronger [title/summary/skill] wording: do you have proof of [capability] with stronger scope, proof, or technical detail?`

Gap severity calibration:
- Use `hard_gate` only when the JD explicitly requires the capability, the role cannot credibly be performed without it, and the candidate lacks a functional substitute.
- Use `near_gate` when the capability is central to hiring-manager confidence but adjacent proof may still earn an interview.
- Use `differentiator` when the evidence would materially improve competitiveness but is not required for recruiter or hiring-manager pass-through.
- Use `interview_prep` when the topic is useful for interviews but should not drive resume positioning.
- Use `must_add_if_true` when the resume should include the story if the candidate truly has it.
- Use `safer_positioning` when missing proof should cap the title, summary, scope bullet, or skills away from the most niche JD-native claim.
- Use `optional_storybank_add` when the story would strengthen the resume but should not block applying.
- Use `interview_prep_only` when the story should be prepared for interviews but not added to the resume.
- Do not overuse `hard_gate`; most useful feedback questions are `near_gate` or `differentiator`.
- Do not mark a broad JD-native phrase as `hard_gate` merely because some exact sub-surfaces are missing.
- If competitive-stretch acceptance supports the broader phrase, use `gaps` to identify bullet-strengthening, replacement, or interview-defense needs rather than forcing safer resume positioning.

Examples of tagged gaps:
- `[gap_type=near_gate | impact=high | action=must_add_if_true | resume_action=strengthen_existing_bullet | target=payment-authentication success bullet] Would strengthen the existing bullet: can you provide the 1-2 workflow or technical details that make the metric improvement more believable?`
- `[gap_type=differentiator | impact=medium | action=optional_storybank_add | resume_action=replace_existing_bullet | target=weakest lower-priority bullet] Could replace a weaker bullet only if strong: do you have a candidate-owned story showing [capability], [customer/user], and [metric/outcome] with concrete proof?`
- `[gap_type=interview_prep | impact=low | action=interview_prep_only | resume_action=interview_prep_only | target=interview defense] Prepare an interview explanation of [boundary/unsupported surface] without forcing it into the resume.`
- `[gap_type=differentiator | impact=high | action=optional_storybank_add | resume_action=summary_title_or_skill_unlock | target=seniority scope] Would unlock positioning at the level this JD implies: do you have a candidate-owned story showing cross-team, multi-product, platform, or org-level scope (teams influenced, products owned, scale) with concrete proof?`

Final context-leak audit:
- Remove any company-wide metric from proof_points, summary_lead, excitement_anchor, and selected bullets unless it appears in candidate-owned bullet/project evidence.
- Remove broad company product lines from bullets unless the candidate-owned source bullet says the candidate owned that product line.
- If the current resume has a company context field, preserve it as context only; do not mine it for bullet facts.
- If you need a scale metric, prefer candidate-owned metrics from role descriptions over employer-wide context metrics.

Final audit before output:

- Technical language is calibrated to the JD and hiring-manager persona: preserve source-supported signal jargon for technical roles; translate noise jargon for less technical roles.
- Original resume technical terms are not stripped when they improve credibility for a technical PM, platform, AI/ML, risk, fraud, security, identity, infra, API, or fintech role.
- Bullets remain understandable even when technical terms are preserved.

- Every high-strength selected storyline has a visible execution path in summary, excitement anchor, first relevant bullets, or a dedicated bullet selection entry.
- Distinct proof categories in the most relevant role are preserved when 4-6 bullets are allowed; do not drop performance/success metrics just because launch/scale is already represented.
- `summary_lead` includes the most differentiated proof from `excitement_anchor` when placement is `summary` or `both`.
- Overloaded bullet selections are simplified before output so Prompt 3 is not asked to reconstruct an overpacked bullet.
- Prompt 2 resolves conflicts between source-bullet selection and guardrails before output instead of passing contradictory instructions to Prompt 3.

- Selected scope bullets use an umbrella category when sub-product lists do not add value, and include team/org scope only when it cleanly improves seniority.
- Compressed current-role instructions remain readable and do not pack multiple proof themes into one bullet.
- Long action lists are reduced to the strongest proof detail or dropped when they add clutter.
- Bullet instructions favor easy-to-understand resume language over product bingo or keyword stuffing.

- Competitive-stretch acceptance is applied: broad JD-native categories are not over-constrained when the candidate can defend them through multiple supported workflow moments.
- Unsupported exact subclaims are blocked without suppressing supported high-value categories.
- Direct JD-responsibility matches are promoted early within the relevant role unless a stronger scope bullet or quantified outcome must lead.
- Reduction metrics are paired with same-role scale/growth metrics when supported.
- Emerging-risk or market-shift stories are elevated when they map to the hiring manager's domain and contain a candidate-owned product decision.
- Bullet instructions identify the strongest supporting proof detail instead of long artifact or process lists.
- No resume-facing field contains hedge/defense words.
- `gaps` include only the top 3-4 resume-actionable questions ranked by expected impact on the final resume.
- Each top gap question states its intended `resume_action` and target bullet/theme.
- New-bullet questions are included only when they can plausibly enter the top 4-6 bullets or replace a weaker current proof category.
- Questions that only help interview defense are tagged `interview_prep_only` or omitted unless high-risk.
- Resume-facing fields have passed the target-native translation pass: source-native labels are supporting proof details, while hiring-manager-native labels lead title, summary, lead bullet, and top skills when defensible.
- Broad JD terms are scope-limited when only part of the broad system/lifecycle/platform is supported.
- Bullet selection entries each target one clear story; they should not bundle several competing stories.
- If the current role is less relevant, Prompt 2 selects exactly 1 bullet for it unless a second bullet has direct/functionally equivalent target-domain proof.
- `summary_lead` stays compact and does not stuff every supported metric into one field.
- Team-scope bullets use clean human wording and are omitted when product/metric bullets already establish seniority.
- Skill labels avoid implying formal program ownership or engineering infrastructure authority unless explicitly supported.
- Company/team-specific labels are converted into market-natural equivalents for `recommended_title` unless the exact label is externally recognizable and natural.
- Scope bullets do not overpack team size, direct reports, product scope, scale, architecture, and revenue.
- Top skills are market-natural and proof-specific, not internal-team labels or generic category labels.
- Gap and guardrail fields are specific enough for Prompt 3 to ask only resume-changing questions, without adding new fields or distracting from the resume strategy.
- No mixed-support JD phrase remains bundled without subclaim-level clarity.
- Every blocked high-value phrase has a positive substitute when one exists.
- The title and summary use the strongest safe JD-native domain language.
- The current role is compressed if less relevant.
- The excitement anchor is memorable, metric-backed, and specific.
- The final brief is generic-process generated from the current JD, not from prior examples.

A++ final promotion audit:
- Identify the strongest 1-2 central JD-native promotion phrases the hiring manager most wants to see.
- If they are direct/functionally equivalent, confirm they appear in `recommended_title`, `summary_lead`, and the lead bullet or another top-2 selected bullet of the most relevant role.
- Identify the next 3-5 most important truth-safe preservation phrases and confirm they remain in `phrases_to_mirror`, relevant bullet selections, or `final_skills_list` so Prompt 3 can preserve them in final resume text.
- If any promotion or preservation phrase is excluded, the reason must be captured in explicit fields such as `cannot_claim`, `signals_to_avoid`, `gaps`, or the relevant guardrail.- If a source-native term crowds out a stronger target-native term, revise the resume-facing field before output.

Natural-language polish audit:
- If `recommended_title`, `summary_lead`, or lead-bullet direction reads like a keyword pile, reduce redundant promoted modifiers before output.

Prompt 2 `flex_notes` discipline:
- Use `flex_notes` only for small non-critical implementation notes for future prompt variants.
- Do not include title rationale, phrase-promotion rationale, include/exclude strategy, bullet strategy, skill strategy, or resume-facing decisions.
- If information affects the final resume, it must appear in explicit fields such as `recommended_title`, `summary_lead`, `selected_storylines`, `bullet_rewrite_instructions`, `final_skills_list`, `cannot_claim`, `skills_to_avoid`, `signals_to_avoid`, or `gaps`.
- Prefer `""` when unused.

Inputs:
Prompt 1 JSON:
{{PROMPT1_JSON}}

Current resume:
{{CURRENT_RESUME}}

Story bank:
{{STORYBOARD}}

Return valid JSON only, exactly matching the Prompt 2 output contract supplied by the system/backend.

A++ final score-gate:
- If `recommended_title` does not contain a validated role-winning JD-native phrase, revise unless a stronger defensibility reason exists.
- If `summary_lead` does not include both the target-native domain and the strongest proof metric, revise.
- If the strongest defensible target-native category does not appear in the lead bullet or within the first 2 selected bullets of the most relevant role, revise.
- If `final_skills_list` top 5 are not JD-native/domain-proof skills, revise.
- If the brief sounds safe but not memorable, revise toward Tier 2/Tier 3 language.
