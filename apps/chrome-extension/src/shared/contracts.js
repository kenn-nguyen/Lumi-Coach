// Shared notes for the extension-side flow.

export const RESUME_MATCHER_NOTES = {
  patchEndpoint: "PATCH /resumes/{resume_id}",
  pdfEndpoint: "GET /resumes/{resume_id}/pdf",
  expectedOutput: "ResumeData JSON"
};
